/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['dummyimage.com', 'res.cloudinary.com', 'images.pexels.com'],
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
